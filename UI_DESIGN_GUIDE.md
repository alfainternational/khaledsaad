# دليل تصميم الواجهات

هذا الدليل يستخرج قواعد الواجهة من الصفحة الرئيسية الحالية ويحوّلها إلى مرجع عملي لتطوير جميع أجزاء الموقع. المرجع البصري الأساسي هو:

- `resources/views/layouts/marketing.blade.php`
- `resources/views/pages/home.blade.php`
- `resources/css/app.css`

القاعدة العامة: الصفحة الرئيسية هي "مصدر الحقيقة" للهوية البصرية، وأي صفحة جديدة يجب أن تنتمي إليها بصرياً قبل أن تحاول إضافة شخصية خاصة بها.

## 1. الشخصية البصرية للموقع

الواجهة الحالية تقدم هوية تجمع بين:

- وضوح استراتيجي لا يبدو تقنياً جافاً.
- طابع عربي حديث مع دعم كامل لـ `RTL`.
- إحساس premium هادئ، وليس مظهراً إعلانياً صاخباً.
- استخدام محسوب للتوهج، الشفافية، والتدرجات اللونية.
- اعتماد واضح على المساحات البيضاء والبطاقات المنفصلة لتقليل التشويش.

لهذا السبب يجب أن تبدو جميع الصفحات:

- نظيفة ومقصودة.
- مركزة على القرار التالي للمستخدم.
- غنية بصرياً ولكن بدون ازدحام.
- متسقة بين الوضع الداكن والفاتح.

## 2. مبادئ التصميم الأساسية

### 2.1 التسلسل قبل الزينة

كل صفحة يجب أن تجيب سريعاً عن:

1. أين أنا؟
2. ماذا سأستفيد هنا؟
3. ما الخطوة التالية؟

لذلك يُبنى كل قسم وفق ترتيب ثابت:

- وسم صغير `Eyebrow` أو `Badge`
- عنوان رئيسي قوي
- وصف قصير واضح
- إجراء رئيسي واحد واضح
- إجراء ثانوي عند الحاجة فقط

### 2.2 البطاقات هي الوحدة الأساسية

الواجهة الحالية لا تعتمد على كتل نصية طويلة، بل على وحدات منفصلة داخل بطاقات. هذا يعني:

- أي فكرة مستقلة أو ميزة أو نتيجة أو حالة يجب أن تظهر داخل `card` أو أحد مشتقاتها.
- لا نستخدم حدوداً ثقيلة أو خلفيات صلبة جداً.
- الحركة الأساسية للبطاقات هي رفع بسيط للأعلى وتكثيف الظل.

### 2.3 إبراز محدود ومدروس

اللون الأساسي والتدرجات اللامعة ليست للاستخدام الحر. تستخدم فقط من أجل:

- الزر الرئيسي
- الكلمات المفتاحية في العناوين
- مؤشرات الحالة
- لمسات الإبراز داخل بعض المكونات

ولا تستخدم من أجل:

- فقرات كاملة
- خلفيات نصية واسعة
- قوائم طويلة

### 2.4 التوازن بين التقنية والإنسانية

لغة التصميم يجب أن توحي بأن المنصة ذكية ومنظمة، لكن قريبة من المستخدم. لذلك:

- الرسوم والمكونات يجب أن تشعر بأنها "منتج عملي" لا "لوحة تحكم تقنية بحتة".
- النصوص مباشرة، مفهومة، وتخاطب القرار والنتيجة.

## 3. نظام الألوان المعتمد

القيم الفعلية موجودة في `resources/css/app.css` ويجب الالتزام بها عبر المتغيرات فقط.

### 3.1 ألوان الهوية

- اللون الأساسي `--p`: `#6366f1`
- اللون الأساسي الداكن `--p-dark`: `#4f46e5`
- اللون الأساسي الفاتح `--p-light`: `#818cf8`
- اللون الثانوي البنفسجي `--p2`: `#8b5cf6`
- اللون المساند الأخضر المزرق `--teal`: `#14b8a6`
- اللون الذهبي `--gold`: `#f59e0b`
- اللون الوردي التحذيري/التمييزي `--rose`: `#f43f5e`

### 3.2 استخدام الألوان

- `--p` و `--p2` للهوية الأساسية والأزرار الرئيسية والتدرجات.
- `--teal` للمؤشرات الذكية، النجاح، والميزات المرتبطة بالأتمتة والتحليل.
- `--gold` للتأكيد على الإنجاز أو القيمة أو المخرجات الخاصة.
- `--rose` للحالات الخاصة أو التمييز المقصود فقط.

### 3.3 الخلفيات والسطوح

في الوضع الفاتح:

- الخلفية العامة: `--bg`
- خلفية بديلة ناعمة: `--bg-2`
- خلفية قسم أو شريط أغمق قليلاً: `--bg-3`

في الوضع الداكن:

- الخلفية يجب أن تبقى عميقة جداً ومائلة للأزرق، لا رمادية مسطحة.
- يعتمد الشكل العام على الشبكة الخلفية والتوهجات الخفيفة، وليس على كتل سوداء صلبة.

### 3.4 قواعد مهمة

- يمنع استخدام ألوان hex مباشرة داخل الصفحات إلا لحالة موثقة ومقصودة جداً.
- أي لون جديد يجب أن يضاف كمتغير في `:root` ونسخته في `[data-theme="dark"]` إذا لزم.
- لا تستخدم ألوان Tailwind المباشرة في صفحات التسويق إذا كان لها بديل داخل النظام الحالي.

## 4. الطباعة

### 4.1 الخط

الخط الأساسي المعتمد هو `Alexandria`، وهو جزء من الهوية الحالية، لذلك:

- يستخدم في جميع صفحات التسويق.
- لا يتم إدخال خط آخر للواجهة العامة بدون قرار تصميمي واضح.

### 4.2 التسلسل الطباعي

التدرج الحالي في `app.css` يجب أن يبقى مرجعاً:

- `heading-xl` للعناوين البطولية فقط.
- `heading-lg` لعناوين الأقسام الرئيسية.
- `heading-md` لعناوين الكتل أو المقدمات الثانوية.
- `heading-sm` للعناوين الداخلية للبطاقات.
- `text-body-lg` للمقدمات الوصفية المهمة.
- `text-body` للنص التشغيلي المعتاد.
- `text-caption` للنصوص المساندة والعلامات الصغيرة.

### 4.3 قواعد الكتابة

- العناوين قصيرة وقوية.
- الوصف لا يتجاوز 2 إلى 4 أسطر غالباً.
- الأسطر الطويلة جداً غير مرغوبة، خصوصاً في العربية.
- التدرج النصي `text-gradient` يستخدم داخل كلمة أو كلمتين فقط، لا على سطر كامل إلا في CTA مميز جداً.

## 5. المسافات والأحجام

النظام الحالي يستخدم سُلّماً واضحاً للمسافات:

- `--sp-1` إلى `--sp-24`

### 5.1 ما الذي يعنيه ذلك عملياً

- المسافة الصغيرة داخل العناصر: `--sp-2` إلى `--sp-4`
- المسافة بين أجزاء المكوّن الواحد: `--sp-4` إلى `--sp-6`
- المسافة بين البطاقات: `--sp-4` إلى `--sp-6`
- المسافة بين الأقسام: `section` أو `section-lg`

### 5.2 قواعد عامة

- لا نكدّس العناصر بصرياً.
- لا نستخدم مسافات عشوائية داخل `style=""`.
- أي spacing متكرر يجب أن يمر عبر الكلاسات الحالية أو عبر متغيرات النظام.

## 6. الحواف والظلال

### 6.1 الحواف

الحواف الدائرية عنصر أساسي في شخصية الموقع:

- العناصر الصغيرة: `--r-sm`
- البطاقات القياسية: `--r-lg`
- البطاقات الرئيسية والأقسام المميزة: `--r-xl`
- الأزرار والشارات: `--r-full`

### 6.2 الظلال

الظلال ناعمة وعميقة وليست حادة:

- `--shadow-sm` للمكونات الهادئة
- `--shadow-md` للبطاقات التفاعلية
- `--shadow-lg` و `--shadow-xl` للعناصر البارزة مثل hero visual و CTA

القاعدة:

- الظل لا يستخدم وحده لإظهار التفاعل، بل مع تحريك خفيف وتغيير border/background.

## 7. بنية الصفحة

### 7.1 الحاوية العامة

جميع صفحات التسويق يجب أن تستخدم:

- `.site-container`

بعرض أقصى `1160px` مع padding أفقي متجاوب.

### 7.2 ترتيب الصفحة التسويقية القياسي

الهيكل الموصى به لمعظم الصفحات:

1. شريط تنقل ثابت
2. قسم افتتاحي Hero أو Intro
3. شريط ثقة أو نقاط اعتماد عند الحاجة
4. قسم نتائج أو فوائد
5. قسم شرح أو آلية عمل
6. بطاقات اختيار/استكشاف
7. CTA نهائي
8. Footer

### 7.3 أنماط الشبكات المعتمدة

- `.hero-layout`
- `.two-col`
- `.two-col-wide`
- `.three-col`
- `.two-col-cards`
- `.four-col`

ولا يفضّل بناء grid جديد لكل صفحة إذا كان أحد هذه الأنماط يفي بالغرض.

## 8. المكونات الأساسية

### 8.1 شريط التنقل

خصائصه الأساسية:

- عائم وشفاف جزئياً
- `backdrop-filter` مع blur
- حواف دائرية كاملة
- يتحول بصرياً عند التمرير

قواعد استخدامه:

- روابط قليلة وواضحة
- زر رئيسي واحد فقط في الـ nav
- زر ثانوي خفيف عند الحاجة
- دعم كامل للموبايل عبر `nav-toggle`

### 8.2 الشارات `Badge`

النمط المعتمد هو `.section-badge` و `.section-badge-text`.

تستخدم الشارة من أجل:

- حالة النظام
- تصنيف القسم
- تقديم الفكرة الرئيسية

ولا تستخدم داخل كل بطاقة بلا داع.

### 8.3 الأزرار

الأنواع الرسمية الحالية:

- `.btn-primary`
- `.btn-secondary`
- `.btn-ghost`
- `.btn-teal`

الأحجام:

- `.btn-sm`
- `.btn-lg`
- `.btn-xl`

قواعد الأزرار:

- في كل شاشة أو قسم: زر رئيسي واحد كحد أقصى في المنطقة نفسها.
- الزر الرئيسي دائماً هو الأوضح بصرياً.
- الزر الثانوي لا ينافسه لونيًا.
- لا نصمم زر جديد إلا إذا كان يمثل سلوكاً جديداً فعلاً.

### 8.4 البطاقات

العائلة الأساسية:

- `.card`
- `.metric-card`
- `.benefit-card`
- `.path-card`
- `.step-card`
- `.testimonial-card`

قواعد موحّدة:

- خلفية شفافة أو ناعمة بحسب الثيم
- حد خفيف
- hover هادئ
- padding مريح
- عنوان واضح ووصف قصير

### 8.5 الأقسام المميزة

الأنماط الحالية التي يجب إعادة استخدامها:

- Hero section
- Trust strip
- Section band
- CTA section
- Studio window mockup

أي صفحة جديدة يجب أن تبني من هذه الأنماط بدل اختراع بنية مختلفة بالكامل.

## 9. الحركة والتفاعل

### 9.1 الحركة الأساسية

الحركة الحالية تعتمد على:

- `reveal`
- `reveal-left`
- `reveal-right`
- تأخيرات `d-1` إلى `d-5`

### 9.2 قواعد الحركة

- الحركة للدخول التدريجي وليست للاستعراض.
- المسافة الحركية قصيرة.
- لا نستخدم bounce أو zoom مبالغ فيه.
- hover يكون غالباً:
  - `translateY(-2px)` إلى `translateY(-6px)`
  - رفع الظل
  - إظهار border أقوى

### 9.3 متى نستخدم التوهج

يستخدم التوهج مع:

- الأزرار الأساسية
- الـ hero visual
- CTA النهائي
- بعض مؤشرات الذكاء أو الحالة

ولا يستخدم كخلفية متكررة في كل الأقسام.

## 10. الاستجابة للشاشات

النظام الحالي يثبت 3 مستويات رئيسية:

- حتى `1024px`
- حتى `768px`
- حتى `480px`

### 10.1 قواعد التحول

- الأعمدة الثنائية تتحول إلى عمود واحد.
- بطاقات الشبكات تقل تدريجياً إلى عمود واحد.
- Hero visual يختفي على التابلت للحفاظ على التركيز.
- أزرار CTA في الموبايل يجب أن تصبح بعرض كامل عند الحاجة.

### 10.2 قواعد تجربة الجوال

- الأولوية للنص والزر الرئيسي.
- لا تكدّس شارات وعناصر ثقة كثيرة في سطر واحد.
- أي card يجب أن يبقى مقروءاً بدون ضغط.

## 11. قواعد المحتوى داخل الواجهة

### 11.1 لغة النصوص

الأسلوب المعتمد في الصفحة الرئيسية:

- مباشر
- مطمئن
- استراتيجي
- يركز على الفائدة والخطوة التالية

لذلك يجب أن تتجنب الصفحات الجديدة:

- اللغة التقنية الجافة
- الوعود التسويقية المبالغ فيها
- الفقرات الطويلة
- العناوين العامة مثل "حلول متكاملة"

### 11.2 بنية النسخ

في أغلب الحالات:

- عنوان: من 4 إلى 10 كلمات
- وصف: من 1 إلى 3 جمل قصيرة
- CTA: فعل واضح ومحدد

### 11.3 الأيقونات والإيموجي

الصفحة الرئيسية تستخدمها بقدر محدود داخل بعض البطاقات فقط. لذلك:

- تستخدم عندما تضيف تمييزاً سريعاً وواضحاً.
- لا تتحول إلى أسلوب زخرفي شامل.

## 12. قواعد تطوير الصفحات الجديدة

### 12.1 صفحات التسويق الأساسية

مثل:

- الرئيسية
- المسارات
- الأدوات
- القوالب
- الاستوديو

يجب أن تبنى من:

- `hero/introduction`
- `section-header`
- card grids
- final CTA

### 12.2 صفحات الميزة أو القسم

الصفحة التعريفية لأي قسم يجب أن تحتوي غالباً على:

1. مقدمة واضحة
2. لماذا هذه الصفحة مهمة
3. 3 إلى 6 فوائد أو استخدامات
4. الإجراء التالي

### 12.3 صفحات البيانات أو التقارير

حتى لو كانت وظيفية أكثر، يجب أن تحافظ على:

- نفس الألوان
- نفس الحواف
- نفس منطق البطاقات
- نفس التسلسل الهرمي للعناوين

لكن يمكن أن تكون أكثر هدوءاً وأقل زينة من الصفحة الرئيسية.

### 12.4 صفحات الحساب والإدارة

هذه الصفحات لا تحتاج نفس مستوى الإبهار، لكنها يجب أن ترث:

- typography
- colors
- card surfaces
- spacing system
- buttons

أي لا يجوز أن تبدو كواجهة مختلفة بالكامل.

## 13. قواعد التنفيذ البرمجية

### 13.1 مصدر الحقيقة

المرجع الأول لأي قرار بصري هو:

- `resources/css/app.css`

وليس الكلاسات العشوائية داخل القوالب.

### 13.2 ممنوعات

- ممنوع بناء واجهات جديدة بأسماء كلاسات غير موثقة إذا كان هناك مكافئ موجود.
- ممنوع الإكثار من `style=""` داخل Blade.
- ممنوع إدخال ألوان أو مسافات عشوائية غير مبنية على tokens.
- ممنوع مزج أسلوب الصفحة الرئيسية مع أسلوب Tailwind افتراضي غير منسجم بصرياً داخل الصفحة نفسها.

### 13.3 المطلوب عند إضافة مكون جديد

إذا احتجنا مكوناً جديداً:

1. نبحث هل يمكن تركيبه من مكوّنات موجودة.
2. إن لم يمكن، نضيفه باسم دلالي داخل `app.css`.
3. نربطه بمتغيرات النظام الحالية.
4. نختبره على الداكن والفاتح.
5. نراجعه على الشاشات الصغيرة.

## 14. قوالب جاهزة مقترحة

### 14.1 مقدمة صفحة داخلية

يستخدم هذا النمط لصفحات: الأدوات، القوالب، التقارير، المشاريع.

- شارة صغيرة
- `heading-lg`
- `text-body-lg`
- صف أزرار
- بطاقة جانبية تلخص الهدف أو الخطوة التالية

### 14.2 قسم فوائد

- `section-header`
- شبكة `three-col` أو `two-col-cards`
- بطاقات قصيرة

### 14.3 قسم مقارنة أو اختيار

- بطاقات `path-card` أو نسخة مشتقة منها
- لكل بطاقة عنوان ووصف وCTA
- تمييز لوني خفيف فقط

### 14.4 CTA نهائي

- يجب أن يستخدم نمط `.cta-section`
- عنوان قوي
- وصف قصير
- زر أساسي واضح
- زر ثانوي واحد فقط

## 15. نماذج تنفيذ سريعة

### 15.1 هيكل صفحة تسويقية داخلية

```blade
@extends('layouts.marketing')

@section('content')
<section class="section-lg">
    <div class="site-container">
        <div class="two-col-wide">
            <div class="reveal-left">
                <div class="section-badge">
                    <span class="section-dot"></span>
                    <span class="section-badge-text">عنوان القسم</span>
                </div>

                <h1 class="heading-lg mb-5">
                    عنوان واضح مع <span class="text-gradient">إبراز محدود</span>
                </h1>

                <p class="text-body-lg mb-6">
                    وصف قصير يشرح فائدة الصفحة وما الذي سيحصل عليه المستخدم هنا.
                </p>

                <div class="flex gap-3">
                    <a href="#" class="btn btn-primary btn-lg">الإجراء الرئيسي</a>
                    <a href="#" class="btn btn-secondary btn-lg">إجراء ثانوي</a>
                </div>
            </div>

            <div class="card reveal-right d-2">
                <h2 class="heading-sm mb-4">ملخص سريع</h2>
                <p class="text-body">
                    استخدم البطاقة الجانبية لتوضيح الهدف أو الخطوة التالية أو أهم نتيجة متوقعة.
                </p>
            </div>
        </div>
    </div>
</section>
@endsection
```

### 15.2 شبكة فوائد أو استخدامات

```blade
<section class="section-lg bg-3">
    <div class="site-container">
        <div class="section-header reveal">
            <p class="text-eyebrow mb-3 text-p">الفوائد</p>
            <h2 class="heading-lg mb-4">لماذا هذا القسم <span class="text-gradient">مهم؟</span></h2>
            <p class="text-body">مقدمة قصيرة تشرح قيمة البطاقات التالية.</p>
        </div>

        <div class="three-col">
            <article class="card reveal d-1">
                <h3 class="heading-sm mb-3">فائدة أولى</h3>
                <p class="text-body">وصف مختصر ومباشر.</p>
            </article>

            <article class="card reveal d-2">
                <h3 class="heading-sm mb-3">فائدة ثانية</h3>
                <p class="text-body">وصف مختصر ومباشر.</p>
            </article>

            <article class="card reveal d-3">
                <h3 class="heading-sm mb-3">فائدة ثالثة</h3>
                <p class="text-body">وصف مختصر ومباشر.</p>
            </article>
        </div>
    </div>
</section>
```

## 16. الوصولية وتجربة الاستخدام

يجب الحفاظ على:

- وضوح النص فوق الخلفية في الداكن والفاتح
- أحجام نقر مريحة للأزرار
- استخدام `aria-label` للعناصر غير النصية
- الحفاظ على ترتيب بصري واضح في `RTL`
- عدم الاعتماد على اللون وحده لشرح الحالة

## 17. ما يجب توحيده لاحقاً

التحليل الحالي يكشف أن الصفحة الرئيسية هي الأكثر نضجاً بصرياً، بينما بعض الصفحات الأخرى ما زالت تستخدم أسلوباً مختلفاً أو غير مكتمل. لذلك يجب اعتماد ما يلي في التطوير القادم:

- الصفحة الرئيسية هي المرجع البصري الرسمي.
- أي صفحة تستخدم كلاسات مثل `panel`, `pill`, `action-primary`, `shell` يجب مراجعتها وربطها بالنظام الحالي أو استبدالها بكلاسات موحدة.
- الأنماط الموجودة في `home.blade.php` يجب تحويلها تدريجياً إلى مكوّنات أو كلاسات قابلة لإعادة الاستخدام بدل تكرارها بصيغ مختلفة.
- أي inline styles موجودة حالياً تعتبر استثناءً مؤقتاً وليست معياراً للتطوير الجديد.

## 18. قائمة تحقق قبل اعتماد أي واجهة جديدة

- هل الصفحة تبدو امتداداً واضحاً للصفحة الرئيسية؟
- هل استخدمت الألوان من tokens فقط؟
- هل العنوان والوصف والـ CTA واضحون خلال أول نظرة؟
- هل استخدمت مكونات النظام بدلاً من بناء مكونات جديدة بلا داع؟
- هل التصميم يعمل على الداكن والفاتح؟
- هل يعمل بشكل جيد على الجوال؟
- هل يوجد عنصر رئيسي بصري واحد فقط في كل منطقة؟
- هل الحركة خفيفة وغير مشتتة؟
- هل النص العربي مقروء ومريح؟

## 19. القرار النهائي

الهوية المعتمدة للموقع هي:

- واجهة عربية حديثة
- داكنة الأساس مع دعم فاتح متكامل
- قائمة على البطاقات
- تستخدم تدرجات وتوهجات محسوبة
- تعتمد على تسلسل هرمي قوي ومسافات واسعة
- توازن بين الحس الاستراتيجي والطابع التقني الذكي

أي تطوير جديد يجب أن يلتزم بهذه الهوية، لا أن يبدأ من الصفر في كل صفحة.
