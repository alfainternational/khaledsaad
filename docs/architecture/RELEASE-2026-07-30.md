# الإصدار الأول الحقيقي — ٢٠٢٦-٠٧-٣٠

> البند السابع والأخير من مرحلة التحوّل في
> [`UNIFIED-DIAGNOSIS-PLAN.md §١١`](UNIFIED-DIAGNOSIS-PLAN.md).
> ما قبله: نسخة احتياطية مجرَّبة · `APP_KEY` مثبَّت · المسح · خريطة الأصناف ·
> شحن التطبيق ورفع الحد · تطوير عقد `v1`.

---

## ١. ما أُعلن

| البند | القيمة |
| --- | --- |
| المنصة | `khaledsaad.net` — الكود مطابق لالتزام `6d9023d` |
| تطبيق أندرويد | `1.0.4` بناء `5` |
| بصمة APK | `7823e9ce20c3c5f8891568bd606afa0be172dedde43e4c808c874e85b698dea7` |
| شهادة الموقِّع | `45b6a8e35aadb87004a11a6efefd8c21e453ce703763f2e93557c3fa5e5a3aff` |
| الحد الأدنى المدعوم | بناء `5` — البوابة **مفعّلة** |
| الهجرات | ٣٧ مُطبَّقة، ولا معلّقة |

المحاور الثمانية تعمل، والمخرجات التسعة تصل الويب والعقد، والسقف التشغيلي
يحجز قبل الطابور وينبّه عند ٨٠٪ ويرفض عند ١٠٠٪.

---

## ٢. ترتيب §١٠.٣ اكتمل — العقد صار قابلًا للتطوير

الشرط اللازم لتطوير `api/v1` في مكانه بلا إصدار ثانٍ:

1. **بوابة الحد الأدنى مشحونة وخاملة.** كانت `EnsureSupportedAppVersion`
   موجودة على الخادم **ولا مسجَّلة في `bootstrap/app.php`** — ملفٌّ بلا
   مستدعٍ، فالبوابة لم تكن تفحص شيئًا. سُجِّلت ونُشرت.
2. **نسخة تطبيق ترسل `X-App-Build`.** `1.0.4+5` منشورة ومتاحة للتنزيل.
3. **رفع الحد.** `MOBILE_MIN_SUPPORTED_BUILD=5`.

مُتحقَّق منه على الإنتاج:

| الطلب | الرد |
| --- | --- |
| بلا ترويسة | `426` مع رسالة عربية ورابط التحديث |
| `X-App-Build: 4` | `426` |
| `X-App-Build: 5` | `200` |

قلب هذا الترتيب كان سيمنع الوصول عن كل نسخة مثبَّتة دفعةً واحدة. رفعُ الحدّ
الآن آمن لأن قاعدة الإنتاج مُسحت ولا مستخدمي تطبيق فيها.

---

## ٣. خطة الرجوع — مجرَّبة لا موصوفة

`deploy/cpanel-push.sh` يأخذ نسخة احتياطية على الخادم **قبل** كل رفع، في
`_deploy_backups/push-<ts>/` بنفس شجرة المسارات.

نسخ هذا الإصدار:

```
_deploy_backups/push-20260730-063433   bootstrap/app.php
_deploy_backups/push-20260730-063644   ثلاث شاشات
_deploy_backups/push-20260730-064047   public/build
_deploy_backups/push-20260730-064247   ١٦ ملفًا (routes وconfig وblade)
_deploy_backups/.env.minbuild.bak      .env قبل رفع الحد
```

**التحقّق الفعلي:** `routes/api.php` في نسخة `064247` بـ٢١٩ سطرًا والحيّ
بـ٢٣٣ — النسخة تحمل ما قبل الرفع فعلًا، لا ملفًّا فارغًا ولا نسخةً من الحالي.

الرجوع:

```bash
cd ~/public_html && cp -r _deploy_backups/push-<ts>/* . && php artisan view:clear && touch .lsphp_restart.txt
```

لإبطال البوابة وحدها بلا رجوع كامل: `MOBILE_MIN_SUPPORTED_BUILD=0` في `.env`
ثم `php artisan config:clear`.

---

## ٤. ما نُشر في هذه الدفعة

| الدفعة | المحتوى |
| --- | --- |
| ١٧ ملفًا | تنبيه سقف الاستعلامات · التعارض يصل المراجعة · `readiness:refresh` · تنظيف السطح · مدقّق اللهجة |
| APK + مانيفست | `1.0.4+5` بحجم ٥٩,١٢٦,٠٧٥ بايت وبصمة مطابقة عند التنزيل من الموقع |
| `bootstrap/app.php` | تسجيل بوابة الإصدار |
| ٣ شاشات | لوحة الوكالة · تقرير الحضور · بطاقة الجاهزية PDF — **كانت غائبة عن الخادم كليًّا** |
| `public/build` | أصول مبنية محليًّا (لا npm على الخادم) |
| ١٦ ملفًا | `routes/api.php` و`routes/web.php` وأربعة `config` وستة قوالب وثلاثة من `AiReadiness` |

### كيف اكتُشف الانحراف

مقارنة `md5sum` لكل ملف قابل للتشغيل بين المحلي والخادم (٤٩٣ ملفًا). أول
مقارنة قالت «صفر انحراف» وكانت **خضراء كاذبة**: `md5sum` على ويندوز يُخرج
`hash *path` فقارنتُ مجموعتين لا تتقاطعان. بعد تطبيع المسار ظهر الانحراف
الحقيقي: ٣ ملفات غائبة و١٦ مختلفة.

**الدرس:** «صفر نتائج» ليس دليل سلامة حتى يُثبت أن الفحص يرى شيئًا أصلًا —
اطبع عدد المتطابقات، لا عدد المختلفات وحده.

---

## ٥. مُتحقَّق منه بعد النشر

- انحراف صفر: ٤٩٣ ملفًا متطابقًا، ولا ملف غائب.
- `platform:preflight` أخضر بخمسة بنوده.
- الصفحات العامة `200`، ومسارات `/app` تعيد `302` إلى الدخول.
- بصمة APK المُنزَّلة من الموقع تطابق المانيفست حرفيًّا.
- الأوامر المجدولة مسجَّلة: `readiness:refresh` · `diagnosis:record` ·
  `brain:collect` · `competitors:discover`.
- ٥١٤ اختبارًا خضراء محليًّا · ٣٦ اختبار Flutter · `analyze` نظيف.

---

## ٦. بنود معلّقة بعد الإصدار

| البند | الحالة |
| --- | --- |
| ٥ ملفات بائدة على الخادم | ✅ **حُذفت** ٢٠٢٦-٠٧-٣٠ بعد نسخها في `_deploy_backups/stale-20260730/`. `prune-classmap` أزال ٤ إدخالات من كل خريطة، والانحراف صار صفرًا: ٤٩٤ ملفًا متطابقًا، ولا زائد ولا ناقص. |
| `APP_KEY` مشترك | ✅ **دُوِّر** ٢٠٢٦-٠٧-٣٠ — انظر §٧. |
| مفاتيح الصوت | `SPEECH_API_KEY` و`SPEECH_COST_PER_MINUTE` غير مضبوطة. بلا الثانية تُحسب تكلفة النسخ صفرًا فيعمل السقف ويكذب تقرير التكلفة. |
| `owned_ratio` | غير محسوب بقرار معماري (§٥ من المواصفة). |
| المرحلة ٤ | مؤجّلة بالتصميم. |

---

## ٧. تدوير مفتاح التطبيق

كان `APP_KEY` واحدًا في التطوير والإنتاج (بصمة `47bb52d16e84`)، فنسخة
التطوير تفكّ بيانات اعتماد بوابات الدفع. الإنتاج الآن على `56f8fc5d8cef`
مستقلًّا.

**ما جعل التدوير ممكنًا:** `platform:reencrypt` — لم تكن هناك أداة تفكّ
بالقديم وتكتب بالجديد، ولا نسخة ثانية من الأسرار، فكان التدوير يعني فقدانها.

المخازن ثلاثة لا واحد. النظر إلى بوابات الدفع وحدها كان سيُبقي مفاتيح
المزوّدين ورموز الأجهزة غير قابلة للفك أول ما تُكتب:

| المخزن | العمود | صفوف الإنتاج |
| --- | --- | --- |
| `payment_gateways` | `credentials` | ٤ |
| `settings` حيث `type=secret` | `value` | ٠ |
| `device_tokens` | `token` | ٠ |

الترتيب المنفَّذ: نسخ `.env` ونسخة SQL للأسرار → مفتاح جديد مع
`APP_PREVIOUS_KEYS` يحمل القديم → تجربة جافة (٤ من ٤ قابلة للفك) →
إعادة التشفير → `preflight` → **حذف `APP_PREVIOUS_KEYS`** → `preflight` ثانيًا.

**الدليل الحاسم هو الفحص الأخير:** البوابات الأربع تُفكّ بعد نزع المفتاح
القديم تمامًا. قبل ذلك كان النجاح غامضًا — القديم يفكّ ما لم يُعد تشفيره.

نقطة الرجوع: `_deploy_backups/.env.keyrotate.bak` و
`_deploy_backups/secrets-20260730.sql`.

أثر متوقّع: كل الجلسات بطلت، فالدخول إلى لوحة الإدارة يبدأ من جديد.
