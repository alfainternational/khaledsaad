# CLAUDE.md

ملف تعليمات المشروع. اقرأه بالكامل قبل أي مهمة، والتزم به حرفياً.

> **التنفيذ:** خطة إعادة التأسيس المعتمدة في
> [`docs/architecture/UNIFIED-DIAGNOSIS-PLAN.md`](docs/architecture/UNIFIED-DIAGNOSIS-PLAN.md).
> هذا الملف يعرّف المنتج؛ ذاك يعرّف كيف يُبنى ومتى.

---

## ١. ما هو هذا المشروع

منصة **تشخيص تسويقي ذكية** للسوق الخليجي والعربي.

المستخدم يسجّل نشاطه مرة واحدة، فيبني له النظام **دماغاً تجارياً** (Business Brain) يتراكم مع الوقت: يعرف سوقه، منافسيه، جمهوره، أرقامه، وما نجح معه وما فشل. كل تفاعل لاحق أذكى من سابقه لأنه مبني على تراكم لا على نقطة صفر.

فوق هذا الدماغ يعمل **محرك تشخيص واحد** يقيس نضج النشاط على عدة محاور، يكشف الفجوات بدليل، ثم يقود إلى الإصلاح.

**القاعدة الحاكمة:**

> المستخدم لا يخرج أبداً بيد فارغة، ولا يخرج أبداً بالحل الكامل مجاناً.

### ما هذا المشروع ليس هو

- ليس مجموعة أدوات تسويقية منفصلة
- ليس مولّد خطط أو محتوى
- ليس لوحة مراقبة لمقياس واحد
- ليس منافساً لـ Semrush أو HubSpot على عرض الميزات

الميزات تُنسخ في أسبوع. الخندق هو: **الدماغ المتراكم + المنهجية الموثّقة + الأصول العربية القطاعية**.

السوق: السعودية والخليج أولاً، ثم بقية العالم العربي. اللغة: العربية RTL في كل شيء.

---

## ٢. المنصة القائمة والعلاقة بها

على `khaledsaad.net` منصة عاملة تحتوي على أدوات تسويقية وتوليد خطط ومحتوى **مجاناً**، وبنية اشتراكات وفوترة جاهزة.

**تنبيه حاسم:** تلك المنصة هي **هذا المستودع نفسه**، لا نظام خارجي. لا يوجد نظامان، ولا تكامل شبكي، ولا موقع ثانٍ. إعادة التأسيس تجري **داخل** هذا التطبيق وقاعدة بياناته ونطاقه.

| موجود ولا يُعاد بناؤه | يُبنى الآن |
| --- | --- |
| توليد الخطط التسويقية | محرك التشخيص المقيس |
| الأدوات والحاسبات التسويقية | الدماغ التجاري |
| الحسابات والمصادقة | محاور القياس الموضوعية |
| الاشتراكات والفوترة | التدقيق التقني والاستطلاع |
| الأكاديمية والمحتوى | التنبيهات والمتابعة الزمنية |

**قاعدة ملزمة:** قبل بناء أي قدرة، تحقق إن كانت موجودة. إن وُجدت، اربط بها ولا تبنِ نسخة ثانية.

### الأثر الاستراتيجي

بما أن توليد الخطة مجاني ومتاح، فهو **ليس منتجاً يُباع**. قيمته الوحيدة أنه يستهلك مخرجات التشخيص، فتصير الخطة مبنية على قياس نشاط المستخدم لا على وصف كتبه بنفسه.

لذلك: الخطة المولّدة تُعرض كـ **نتيجة تابعة**، ولا تظهر في أي مكان كمقترح قيمة رئيسي. الرسالة والواجهة تُبنى على **التشخيص والفجوة والإصلاح**.

---

## ٣. الأصول التي تصنع الخندق

ليست ميزات، بل أصول تتراكم. كل قرار هندسي يجب أن يزيدها.

| الأصل | الوصف | ينمو بـ |
| --- | --- | --- |
| `Brain` | حالة مستمرة لكل نشاط | كل تفاعل ومخرج |
| بنك الأسئلة القطاعي | أسئلة شرائية عربية حقيقية بلهجات خليجية | كل عميل في القطاع |
| خريطة المصادر العربية | أي المصادر تستشهد بها النماذج في كل قطاع | كل دورة استطلاع |
| معايير القطاع | متوسطات النضج والأداء | كل تشخيص مكتمل |
| المنهجية الموثّقة | إطار التشخيص والتسمية والتسلسل | تحرير بشري |

خريطة المصادر وبنك الأسئلة لا يملكهما أي منافس عالمي، ولا يمكن بناؤهما بـ prompt.

---

## ٤. المبادئ غير القابلة للتفاوض

أي كود أو نص واجهة يخالف هذا القسم = خطأ يجب رفضه.

### ٤.١ تدرّج الدليل — يسري على كل مخرجات النظام

| المستوى | التعريف | مثال |
| --- | --- | --- |
| `measured` | رُصد فعلياً من مصدر | «ذُكرت في ٣ من ٥ محاولات» |
| `derived` | حساب من بيانات مرصودة | «حصة الصوت ١٢٪» |
| `inferred` | فرضية أو ارتباط أو رأي منهجي | «تموضعك غير واضح» |

هذا المبدأ في التشخيص التسويقي أهم منه في أي قياس تقني، لأن أغلب مخرجاته `inferred` بطبيعتها. **الخطر ليس أن تكون فرضية — الخطر أن تُعرض كحقيقة.**

ممنوع كتابة جملة سببية بصيغة الجزم في أي محور.

**مفردة واحدة لا رابع لها.** لا `high/medium/low`، ولا `estimated/unknown`، ولا درجة يقين رقمية ثانية بجانبها.

### ٤.٢ لا قياس من عيّنة واحدة

أي رقم من استعلام نموذج لغوي يحتاج ٣ محاولات على الأقل، ويُعرض معه عددها. ممنوع «ظاهر / غائب» — الصيغة الصحيحة «٢ من ٥». أي مقارنة زمنية تحتاج نافذة ٤ أسابيع.

### ٤.٣ الفجوات تُعلن ولا تُخفى

إذا نقصت بيانات محور، تُعرض التغطية صراحة. ممنوع تعبئة الفراغ بتقدير صامت.

### ٤.٤ سقف تكلفة صارم

كل **مساحة عمل** لها ميزانية استعلامات شهرية، تُوزَّع حصصاً على مشاريعها. عند ٨٠٪ تنبيه، وعند ١٠٠٪ توقف تلقائي. الحجز يتم **قبل** إدخال المهمة إلى الطابور، لا بعد الاستدعاء. لا استعلام واحد خارج الميزانية.

### ٤.٥ المخرج ليس ملفاً

الخطة الميتة في مجلد التنزيلات فشل منتج. كل مخرج يظل حياً: قابلاً للتحديث، مربوطاً بمهام، ومقيَّماً دورياً.

---

## ٥. محاور التشخيص

المحرك واحد. ما يختلف هو المحاور المفعّلة وعمقها. لكل محور: درجة، تصنيف دليل، مقارنة بمعيار القطاع، وقائمة فجوات.

| # | المحور | ماذا يقيس | مصدر الدليل |
| --- | --- | --- | --- |
| ١ | الوضوح الاستراتيجي | لمن يبيع ولماذا يُشترى منه | استقبال |
| ٢ | فهم الجمهور | دقة تعريف الشرائح | استقبال + إشارات |
| ٣ | التموضع والرسالة | التمايز ووضوح العرض | استقبال + منافسون |
| ٤ | البنية القنواتية | ملاءمة القنوات لرحلة الشراء | استقبال + بيانات |
| ٥ | القياس والبيانات | هل يعرف ما ينجح فعلاً | ربط أدوات |
| ٦ | القدرة التنفيذية | الفريق والعمليات والإيقاع | استقبال |
| ٧ | **الجاهزية للذكاء الاصطناعي** | الظهور في إجابات النماذج + قابلية القراءة الآلية | تدقيق + استطلاع |
| ٨ | **الأصول المملوكة** | الاعتماد على جماهير مستأجرة مقابل بيانات مباشرة | ربط أدوات |

المحاور ١–٦ `inferred` في أغلبها ومصدرها ما يقوله المستخدم. المحوران ٧ و٨ `measured` ومصدرهما بيانات مستقلة عنه.

**هذا الفرق هو أساس التسعير:** ما يُقاس موضوعياً يُباع، وما يُستنتج من كلام المستخدم يُقدَّم مجاناً أو كطبقة داعمة.

---

## ٦. المخرجات

ما يخرج به العميل فعلياً. كل مخرج يجب أن يمكّن قراراً محدداً — مخرج لا يقود لقرار يُحذف.

| المخرج | محتواه | القرار الذي يمكّنه |
| --- | --- | --- |
| درجة النضج التسويقي | رقم من ١٠٠ + ٨ درجات فرعية + موقعك من متوسط القطاع | مشكلتي في الاستراتيجية أم التنفيذ |
| تقرير التشخيص | لكل محور: الدرجة، الفجوات، الدليل، درجة اليقين | من أين أبدأ |
| بطاقة الجاهزية التقنية | فحص بند بند: Schema، فيد المنتجات، الأسعار المقروءة، السياسات، بنية الصفحات العربية | ما أصلحه هذا الأسبوع |
| تقرير الزحف | أي بوت زار، متى، أي صفحات، ما استُبعد | هل أنا مرئي للنماذج أصلاً |
| تقرير الحضور في الإجابات | معدل الذكر، حصة الصوت، تفصيل سؤال بسؤال، من ظهر بدلاً منك | هل أخسر عملاء قبل أن يعرفوني |
| خريطة المصادر | المصادر التي تستشهد بها النماذج في القطاع، مرتبة بالوزن | أين أنشر لأظهر |
| ملف المنافسين | من هم، أين يظهرون، بأي رسالة وعرض | على ماذا أنافس |
| قائمة الإصلاح | كل فجوة مرتبة على محورين: الأثر × الجهد | ما أعمله أولاً |
| التنبيهات | تغيّر في الدرجة أو حركة منافس | متى أتدخل |

**المخرج المتكرر الوحيد هو التنبيه.** كل ما عداه يُباع مرة واحدة. أي قرار منتج يضعف التنبيه يضعف الإيراد المتكرر كله.

### توزيع المخرجات على المستويات

| المستوى | ما يحصل عليه |
| --- | --- |
| ٠ | درجة النضج + أعلى ٣ فجوات **بالاسم دون الحل** + مقارنة بمنافس واحد |
| ١ | التقرير الكامل + بطاقة الجاهزية + تقرير الزحف + لوحة حية + تنبيهات |
| ٢ | ما سبق + مراجعة بشرية + جلسة + خطة ٩٠ يوماً مربوطة بمهام |
| ٣ | تشخيص عميق + ورش + حوكمة + تنفيذ |
| وكالة | جدول محفظة العملاء ودرجاتهم واتجاهها + تصدير بعلامة الوكالة |

المستوى ٠ يخلق فجوة معرفية ولا يقفلها. أي زيادة عليه تقتل التحويل.

---

## ٧. المستويات

| المستوى | المستخدم | التدخل البشري |
| --- | --- | --- |
| ٠ — المستكشف | لا يعرف ما ينقصه | صفر |
| ١ — المنفّذ | مسوّق أو صاحب نشاط | صفر — اشتراك |
| ٢ — النامي | شركة صغيرة بميزانية | مراجعة + جلسة |
| ٣ — المؤسسي | شركة أو جهة | كامل |
| و — الوكالة | وكالة تدير عملاء | حسب الاتفاق |

المستوى ٠ يموّل المستوى ٣: التقرير يكشف الفجوة، والفجوة تحتاج عملاً.

طبقة الوكالة هي الأهم تجارياً — الوكالة ترى الاشتراك تكلفة اكتساب لا مصروفاً، فتدفع بانتظام.

---

## ٨. القدرات (Modules)

```
app/Modules/
  Brain/          الدماغ التجاري — حالة الحساب المتراكمة
  Intake/         الاستقبال التكيفي (نص، ثم صوت عربي)
  Diagnosis/      حساب المحاور ودرجة النضج
  Competitors/    رصد وتحليل المنافسين
  AiReadiness/    التدقيق التقني + استطلاع النماذج (المحور ٧)
  OwnedAssets/    قياس الأصول المملوكة (المحور ٨)
  Measurement/    ربط الأدوات وقياس الأثر
  Alerts/         التنبيهات والمتابعة الزمنية
  Execution/      المهام والتنفيذ والمتابعة
  Reporting/      التقارير العربية RTL
  PlatformBridge/ حدّ ضيق نحو القدرات القديمة فقط
  Shared/         العقود والقيم المشتركة
```

قواعد ملزمة:

- `Diagnosis` و`Measurement` **لا يتصلان بالإنترنت**. يقرآن من قاعدة البيانات فقط ليكونا قابلين لإعادة الحساب والاختبار.
- كل قدرة تكتب في `Brain` عبر واجهة واحدة، ولا تكتب في جداول قدرة أخرى.
- كل مزوّد خارجي خلف interface. لا استدعاء SDK مباشرة من خدمة.
- كل عملية خارجية داخل Job في طابور.
- **لا يوجد `Planning` module.** توليد الخطط يُستدعى عبر `PlatformBridge` مع تمرير مخرجات التشخيص كمدخلات.

### حدّ `PlatformBridge`

| تحت الجسر | خارج الجسر |
| --- | --- |
| مولّد الخطط القائم | `Entitlements` و`FeatureKey` |
| مولّدات المحتوى القديمة | الاشتراكات والمحافظ والفوترة |
| الأدوات المتقاعدة أثناء انتقالها | الملكية و`Workspace` و`Project` |

الجسر يحرس **قاعدة منتج** (ألّا تصير الخطة مقترح قيمة)، لا حدّاً شبكياً. الفوترة والصلاحيات لها واجهتها بالفعل، ولفّها طبقة بلا حارس.

---

## ٩. الدماغ التجاري

أهم بنية في النظام. ليس جدولاً بل طبقة.

```
brain_facts       project_id, key, value_json, evidence_level,
                  source_module, source_reference, observed_at, superseded_by
brain_snapshots   project_id, taken_at, payload, schema_version
brain_events      project_id, type, body, outcome, occurred_at
```

- **لا تُحذف حقيقة، تُستبدل.** التاريخ هو ما يجعل الدماغ ذكياً.
- كل حقيقة تحمل `evidence_level` من ٤.١ ومصدرها.
- عند تعارض مصدرين، يُحفظ الاثنان ويُعلَّم التعارض للمراجعة — لا يُحسم صامتاً.
- أي قدرة تحتاج سياقاً تقرأ من `Brain`، ولا تعيد سؤال المستخدم عما يعرفه النظام.
- تُحفظ لقطة `Brain` مع كل تشخيص حتى يمكن إعادة إنتاج الدرجة لاحقاً.

**التسمية:** `Project` هو النشاط التجاري وهو مفتاح جداول الدماغ. `Workspace` حاوية الملكية والفوترة وهو مالك ميزانية الاستعلامات. كلمة «حساب» لا تُستخدم لأنها كانت تعني الاثنين معاً.

---

## ١٠. سياسة مصادر البيانات

**الفئة أ — مسموح، مجاني، موثوق**
صفحات موقع العميل · سجلات زحف بوتات الذكاء الاصطناعي · Google Search Console · Google Business Profile · Google Trends · فيد المنتجات وSchema · مدخلات الاستقبال · بيانات المنصة القائمة

**الفئة ب — مسموح بميزانية**
APIs النماذج (OpenAI · Gemini · Anthropic · Perplexity) · Google Places API

**الفئة ج — مؤجّلة، لا تُبنى الآن**

| المصدر | السبب |
| --- | --- |
| Google AI Overviews / AI Mode | لا API رسمي. الالتقاط عبر سحب أو مزوّد خارجي، ونسبة نجاح أفضل مزوّد لا تتجاوز ٦٨٪. منطقة قانونية رمادية. |
| مكتبات إعلانات ميتا وتيك توك وجوجل | لا API كامل، والسحب هش ويتكسر مع أي تغيير |

خدمة `AdLibraries` القائمة تبقى **دليل روابط عامة فقط** ولا تدخل مصدراً للتشخيص.

إذا طُلب بناء شيء من الفئة ج، **توقف واسأل** قبل الكتابة.

---

## ١١. المراحل

التفصيل الكامل وبوابات القبول في `docs/architecture/UNIFIED-DIAGNOSIS-PLAN.md §١١`.

### المرحلة ٠ — التوحيد

توحيد مفردة الدليل · تثبيت أسماء المقاييس · نقل الخدمات إلى `app/Modules` · بناء `Brain` · الاختبارات المعمارية. بلا ميزة يراها المستخدم، وبدونها كل ما بعدها موازٍ.

### المرحلة ١ — الأساس + الإسفين

تكلفة تشغيل شبه صفر. لا استدعاء نماذج.

`Brain` بحد أدنى · `Intake` نصي · `AiReadiness` الشق التقني فقط · تحليل سجلات الزحف · `Diagnosis` بمحورين (٧ و٨) · بطاقة الجاهزية + تقرير الزحف بصيغة PDF عربي RTL · بوابة الحد الأدنى لإصدار التطبيق.

تُباع كمشروع بسعر ثابت وتثبت القيمة بلا تكلفة متغيرة.

### المرحلة ٢ — التشخيص الكامل

المحاور ١–٦ · `Alerts` · اللوحة الحية · المستويان ٠ و١ · ربط مخرجات التشخيص بمولّد الخطط القائم.

### مرحلة التحوّل

نسخة احتياطية مجرَّبة · تثبيت `APP_KEY` · مسح البيانات التجريبية بأمر مُدقَّق · شحن التطبيق ورفع الحد الأدنى · تغيير عقد `api/v1` · إعلان الإصدار الأول.

### المرحلة ٣ — الاستطلاع والتوسّع

استطلاع النماذج بميزانية · `Competitors` · خريطة المصادر · لوحة الوكالة · الاستقبال الصوتي.

### المرحلة ٤ — مؤجّلة

AI Overviews · مكتبات الإعلانات · قياس الأثر المتقدم.

**قاعدة:** لا تكتب كوداً لمرحلة لاحقة حتى لو بدا «سهلاً نضيفه الآن». اترك نقاط التوسّع مفتوحة فقط.

---

## ١٢. التعريفات الرسمية

استخدم هذه الأسماء حرفياً في الكود وقاعدة البيانات والواجهة. لا مرادفات.
المرجع المُنفَّذ: `App\Modules\Shared\Metrics\MetricKey`.

```
maturity_score    = المتوسط الموزون لدرجات المحاور المفعّلة (0–100)
axis_score        = درجة محور واحد (0–100)
axis_coverage     = نسبة المدخلات المتوفرة لحساب المحور
mention_rate      = المحاولات التي ذُكرت فيها العلامة
                    ÷ (الأسئلة × المحاولات لكل سؤال)
share_of_voice    = ذكر العلامة ÷ مجموع ذكر كل العلامات
consistency       = ظهور السؤال الواحد ÷ محاولاته
citation_rate     = مرات ربط الموقع كمصدر ÷ مرات الذكر
owned_ratio       = جهات الاتصال المملوكة ÷ إجمالي الجمهور المتاح
readiness_score   = درجة التدقيق التقني (0–100)
```

لا تخلط `mention_rate` بـ `share_of_voice` في أي شاشة. إن عُرضا معاً فبتسميتين ظاهرتين.

---

## ١٣. قواعد الواجهة

- RTL افتراضي. `dir="rtl"` على الجذر
- العربية فقط عدا أسماء المنصات
- لهجة بيضاء بلمسة خليجية. لا فصحى تقريرية جافة، ولا عامية ثقيلة
- كل رقم يُعرض معه أساسه: «١٢٪ من ٦٢٠ استعلام»
- كل مخرج `inferred` يحمل وسماً بصرياً وكلمة «فرضية»
- كل شاشة برقم متغيّر تعرض آخر تحديث ونسبة التغطية
- لا رسم بياني لأقل من ٤ نقاط زمنية
- الفجوة تُوصف بوضوح دون مبالغة تخويفية — الرقم المنخفض يكفي وحده
- **الخطة المولّدة لا تظهر كعنوان أو مقترح قيمة رئيسي في أي شاشة**

---

## ١٤. قواعد الكود

- **Laravel 13** · PHP 8.3 · MySQL 8 · بنية modular
- أسماء المقاييس مطابقة حرفياً للقسم ١٢
- كل حساب مقياس له اختبار وحدة ببيانات ثابتة
- ممنوع حساب مقياس في Blade أو Controller — فقط في `Diagnosis` أو `Measurement`
- كل استدعاء خارجي: timeout صريح، إعادة محاولة محدودة، تسجيل التكلفة
- `raw_response` يُحفظ كاملاً دائماً — التصنيف قد يتغير، والنص الخام هو الدليل
- Migrations لا تُعدَّل بعد التشغيل، تُضاف جديدة. ممنوع `migrate:fresh`
- **لا إصدار API ثانٍ.** يتطوّر `api/v1` في مكانه، محروساً ببوابة حد أدنى لإصدار التطبيق تُبنى وتُشحن قبل أي تغيير في العقد
- أي استخدام لقدرة قديمة يمر عبر `PlatformBridge` ضمن حدّه المعرَّف في §٨

> **ملاحظة إصدار:** اعتُمد Laravel 13 رسمياً في ٢٠٢٦-٠٧-٢٨ بقرار المالك. المستودع
> مثبَّت على `laravel/framework ^13.0` (`v13.21.1`). أي نص سابق يذكر Laravel 12 ملغى.

---

## ١٥. ما لا يجب فعله

- لا تبنِ ما هو موجود — اربط به
- لا تبنِ واجهة جميلة قبل أن تكون البيانات صحيحة
- لا تعرض رقماً لا تعرف كيف حُسب
- لا تحوّل `inferred` إلى `measured` لتحسين شكل التقرير
- لا تعِد سؤال المستخدم عما يعرفه `Brain`
- لا تبنِ مخرجاً لا يقود لقرار محدد
- لا تضف مزوّد بيانات دون تحديث القسم ١٠
- لا تستدعِ نموذجاً لغوياً في المرحلة ١
- لا تفترض أن الترجمة من الإنجليزية تصلح كسؤال عربي — أسئلة البنك تُكتب بلسان مشترٍ حقيقي
- لا تبنِ قدرة منفصلة عن `Brain` — القدرة التي لا تغذّي الدماغ ولا تقرأ منه لا مكان لها هنا
- لا تسوّق الخطة المولّدة كقيمة — هي متاحة مجاناً وقيمتها من التشخيص الذي سبقها
- لا تنشئ نظاماً موازياً بأي صورة: لا موقع ثانٍ، ولا لوحة بديلة، ولا مصدر حقيقة مكرر، ولا إصدار API ثانٍ

---

## ١٦. عند الشك

إذا تعارضت مهمة مطلوبة مع القسم ٤، توقف واذكر التعارض بدل الالتفاف عليه.

المنتج هو **دقة التشخيص وتراكم الدماغ**. كل ما عداهما واجهة.
