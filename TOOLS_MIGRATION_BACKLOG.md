# Backlog نقل أدوات القديم إلى الحالي

## الهدف
نقل أفضل عناصر تجربة الأدوات من `oldkhaledsaad` إلى `khaledsaad` بشكل تدريجي منخفض المخاطر، مع الحفاظ على الهيكل الحالي للأدوات.

## الدفعة 1 (منخفضة المخاطر) — سريعة الأثر

### 1.1 تحسين القراءة داخل شاشة الأداة
- [x] تحويل الشرح التفصيلي إلى قسم اختياري (Progressive Disclosure).
- [x] تقليل المعلومات المرئية لحظة الدخول.
- [x] تثبيت CTA رئيسي واحد لحظة التنفيذ.

### 1.2 ضبط لغة الأداة
- [x] إعداد مرجع لغوي موحد في `TOOLS_UX_CONSTITUTION.md`.
- [x] مراجعة placeholders داخل شاشة التشغيل الحالية وجعل الحقول الإضافية اختيارية وواضحة.
- [x] مراجعة رسائل النجاح/الخطأ في مسار التشغيل وتوحيدها عبر `ToolCopyCatalog`.

### 1.3 قياسات نجاح الدفعة
- [ ] انخفاض الوقت حتى أول تشغيل ناجح.
- [ ] انخفاض الرجوع للخلف من شاشة الأداة بدون حفظ.
- [ ] ارتفاع نسبة إكمال المدخلات الأساسية.

## الدفعة 2 (متوسطة المخاطر) — ذكاء الانتقال بين المستويات

### 2.1 تفعيل سياسة مستويات مدفوعة بالسلوك
- [x] إضافة سياسة مستويات موحدة عبر `ToolModePolicy`.
- [x] تطبيق منع الانتقال لمستويات أعلى بدون جاهزية.
- [x] إظهار سبب القفل للمستخدم بلغة واضحة.

### 2.2 تحسين التوصية بالمستوى
- [ ] ربط التوصية بتاريخ آخر 3 تشغيلات بدل آخر تشغيل فقط.
- [ ] إدخال مؤشرات جودة المدخلات (طول/توازن/اكتمال) في قرار التوصية.
- [ ] دعم رسالة "لماذا نوصي بهذا المستوى الآن؟" داخل الواجهة.

### 2.3 قياسات نجاح الدفعة
- [ ] ارتفاع التحول من `guided` إلى `structured` مع جودة مخرج أعلى.
- [ ] انخفاض حالات الانتقال الخاطئ إلى `expert`.

## الدفعة 3 (متقدمة) — استعادة ثراء القديم بدون تعقيد

### 3.1 مكتبة توسع اختيارية للخبراء
- [ ] إضافة "مكتبة متقدمة" غير ظاهرة افتراضيًا للمستخدم المبتدئ.
- [ ] ربط كل أداة متقدمة بشروط إظهار واضحة (entitlement + readiness).

### 3.2 ربط أقوى بين الأدوات
- [ ] إظهار "السياق القادم" لكل أداة (ما البيانات التي ستغذي الأداة التالية).
- [ ] تحسين next-actions لتكون مرتبطة مباشرة بأداة محددة ومسار واضح.

### 3.3 قياسات نجاح الدفعة
- [ ] زيادة نسبة إكمال السلسلة بين الأدوات المرتبطة.
- [ ] ارتفاع معدل العودة للمخرجات السابقة وإعادة استخدامها.

## ملفات التنفيذ الأساسية
- السياسات والتشغيل:
  - `app/Support/Tooling/ToolModePolicy.php`
  - `app/Http/Controllers/Web/ToolRunController.php`
- تجربة العرض:
  - `resources/views/app/tools/show.blade.php`
  - `app/Http/Controllers/Web/ToolController.php`
- وثائق المنتج:
  - `TOOLS_OLD_VS_CURRENT_AUDIT.md`
  - `TOOLS_UX_CONSTITUTION.md`

## قاعدة الإطلاق
- كل دفعة تنفذ خلف قياس واضح.
- لا الانتقال لدفعة أعلى قبل تحقق مؤشرات الدفعة الحالية.
- أي تحسين لا يرفع وضوح الأداة أو جودة المخرج يعتبر خارج النطاق.
