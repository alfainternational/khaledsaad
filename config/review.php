<?php

return [
    /*
     * المراجعة البشرية — أقوى ما يميّز المنتج، وأسرع ما يفقد الثقة.
     *
     * «وعدٌ بمراجعة بشرية يتأخر يضرّ أكثر من عدم عرضه»: من لم يُوعَد ينتظر
     * بلا سقف، ومن وُعِد بيومين ثم انتظر أسبوعًا يقرأ التأخير خذلانًا لا
     * ازدحامًا. لذلك المدة **معلنة** وتُقاس، والاستقبال يتوقف قبل أن
     * يتحول الطابور إلى وعدٍ لا يُوفى.
     *
     * القيم هنا افتراضات تشغيلية تُضبط من البيئة. القرار الفعلي في
     * `docs/platform/16-open-decisions.md` قرار ٤.
     */
    'sla_hours' => (int) env('REVIEW_SLA_HOURS', 48),

    /*
     * أقصى عدد طلبات مفتوحة قبل إيقاف الاستقبال مؤقتًا.
     *
     * صفر يعني بلا سقف — وهو أخطر إعداد في هذا الملف: طابورٌ بلا سقف
     * يقبل الطلب المئة كما يقبل الأول، ويعد صاحبه بالمدة نفسها.
     */
    'max_open' => (int) env('REVIEW_MAX_OPEN', 20),

    /*
     * عتبة التحذير في اللوحة: نسبة من السقف يبدأ عندها القلق قبل الامتلاء.
     */
    'warn_ratio' => (float) env('REVIEW_WARN_RATIO', 0.8),
];
