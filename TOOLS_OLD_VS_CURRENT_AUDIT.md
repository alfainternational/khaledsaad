# مراجعة أدوات الموقع القديم مقابل الحالي

## الهدف
- الحفاظ على ثراء الأدوات وفوائدها.
- تبسيط تجربة الاستخدام للمستخدم بدون هدم هيكل الأدوات الحالي.
- جعل لغة الأداة مباشرة، دقيقة، وقليلة التشتيت.

## مصادر المقارنة
- القديم: `d:\xampp\htdocs\oldkhaledsaad`
  - `app\Http\Controllers\Dashboard\ToolController.php`
  - `app\Services\ToolRegistryService.php`
  - `app\Support\PlatformCatalog.php`
  - `resources\views\dashboard\tools\_tool.blade.php`
- الحالي: `d:\xampp\htdocs\khaledsaad`
  - `app\Support\Tooling\ToolBlueprintCatalog.php`
  - `app\Support\Dashboard\ToolExperienceResolver.php`
  - `resources\views\app\tools\show.blade.php`
  - `app\Http\Controllers\Web\ToolController.php`

## الخلاصة التنفيذية
- **الموقع القديم** غني جدًا من حيث عدد الأدوات والتصنيفات، لكنه أعلى تعقيدًا في الصيانة، وفيه تشعب أكبر في مصادر تعريف الأداة.
- **الموقع الحالي** أوضح في اللغة وأبسط في التدفق، ويملك بنية أكثر اتساقًا عبر `ToolBlueprintCatalog`، لكنه يحتاج تحسينات إضافية لاستعادة بعض ثراء القديم بشكل متدرج.

## الفروقات التقنية الجوهرية

### 1) مصدر تعريف الأدوات
- القديم: تعريفات موزعة بين أكثر من طبقة (Controller + Service + Catalog + Seeder).
- الحالي: تعريف مركزي أوضح في `ToolBlueprintCatalog`.
- الأثر: الحالي أقل مخاطرة من ناحية الاتساق وأسهل للتطوير المرحلي.

### 2) العمق المعرفي داخل الأداة
- القديم: عمق أفقي (أدوات أكثر وتفرعات أكثر).
- الحالي: عمق رأسي داخل الأداة نفسها (`guided/structured/expert`).
- الأثر: الحالي أفضل للمستخدم الجديد، ويحتاج فقط ضبط انتقالات المستوى للمستخدم المتقدم.

### 3) تجربة القراءة والتنفيذ
- القديم: معلومات غنية جدًا داخل شاشة الأداة، مع احتمال حمل معرفي زائد.
- الحالي: تجربة أنظف، لكنها تحتاج المزيد من الإظهار المتدرج للمعلومات (Progressive Disclosure).

### 4) التخصيص حسب حالة المستخدم
- الحالي أقوى في الاستفادة من `persona/awareness/goal` عند بناء التجربة.
- المطلوب: ربط هذا التخصيص بسلوك الاستخدام الفعلي (عدد التشغيلات، درجة الاكتمال، وتاريخ المخرجات).

## قرارات الدمج المعتمدة
- عدم نقل تشعب القديم كما هو.
- اعتماد الهيكل الحالي كقاعدة ثابتة:
  - `ToolBlueprintCatalog` لتعريف الحقول والمخرجات.
  - `ToolExperienceResolver` لتخصيص التجربة.
  - `ToolRun` كسجل تشغيل ومصدر لسلوك المستخدم.
- استعادة عناصر القوة من القديم تدريجيًا:
  - ملخص حي.
  - خطوات تالية واضحة.
  - ربط أفضل بالأداة التالية.

## مبادئ النجاح
- أي عنصر لا يؤثر في قرار المستخدم داخل لحظة التنفيذ يجب إخفاؤه أو تأجيله.
- الأداة يجب أن تبقى غنية بالمخرجات، لكن واجهتها خفيفة وواضحة.
- كل مستوى معرفي يجب أن يرى ما يناسبه فقط، مع إمكانية التدرج.
